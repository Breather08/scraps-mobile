import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  wrapper: {
    paddingTop: 16,
  },
  title: {
    fontSize: 24,
    fontWeight: "800",
  },
  available: {
    alignSelf: "center",
  },
  btn: {
    color: "#fff",
  },
});

export default styles;
